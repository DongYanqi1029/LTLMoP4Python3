
-- LTL specification file
-- (Generated by the LTLMoP toolkit)

LTLSPEC -- Assumptions
	(
	[](TRUE) & []<>(TRUE)& 
		!e.sensor1
	);

LTLSPEC -- Guarantees
	(
		[] ( (!next(s.bit0) & next(s.bit1)) -> next(s.actuator1) ) & 
		[]<>((s.bit0 & !s.bit1)) & 
		[]<>((!s.bit0 & s.bit1)) & 
		[]<>((!s.bit0 & !s.bit1))
&
			( (!s.bit0 & !s.bit1) 
				 | (!s.bit0 & s.bit1)
				 | (s.bit0 & !s.bit1)
			) 

&
			 []( ((!s.bit0 & !s.bit1)) -> ( ((!next(s.bit0) & !next(s.bit1)))
									| ((!next(s.bit0) & next(s.bit1))) 
									| ((next(s.bit0) & !next(s.bit1)))  ) )  & 
			 []( ((!s.bit0 & s.bit1)) -> ( ((!next(s.bit0) & next(s.bit1)))
									| ((!next(s.bit0) & !next(s.bit1))) 
									| ((next(s.bit0) & !next(s.bit1)))  ) )  & 
			 []( ((s.bit0 & !s.bit1)) -> ( ((next(s.bit0) & !next(s.bit1)))
									| ((!next(s.bit0) & !next(s.bit1))) 
									| ((!next(s.bit0) & next(s.bit1)))  ) )  & 
[]			( (!s.bit0 & !s.bit1) 
				 | (!s.bit0 & s.bit1)
				 | (s.bit0 & !s.bit1)
			) 

	);
